{
	"name": "sts-llm-wiki",
	"version": "0.2.0",
	"description": "Obsidian plugin that embeds a coding agent (pi or Claude Code) as a chat panel operating on your vault.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"deploy": "node deploy.mjs",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"keywords": [
		"obsidian",
		"pi",
		"coding-agent",
		"llm",
		"ai"
	],
	"author": "Stephan Smola",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^20.11.0",
		"builtin-modules": "^3.3.0",
		"esbuild": "0.20.2",
		"obsidian": "latest",
		"typescript": "^5.4.0"
	},
	"allowScripts": {
		"esbuild@0.20.2": true
	}
}
